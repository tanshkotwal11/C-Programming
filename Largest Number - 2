#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int A, B, C;
  
    scanf("%d %d %d", &A, &B, &C);
    int largest = A;
if (B > largest)
        largest = B;
    if (C > largest)
        largest = C;
    printf("%d\n", largest);

    return 0;
}

